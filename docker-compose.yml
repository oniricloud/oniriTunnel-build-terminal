version: '3.8'

services:
  oniri-tunnel:
    build: .
    container_name: oniri-tunnel
    restart: unless-stopped
    network_mode: host
    environment:
      - ONIRI_SEED=${ONIRI_SEED}
      - ONIRI_PASSWORD=${ONIRI_PASSWORD}
    volumes:
      - oniri-config:/root/.oniri
      - oniri-logs:/root/.oniri/logs
    # Note: Port 8777 (HTTP monitoring) is automatically available via host network

volumes:
  oniri-config:
    driver: local
  oniri-logs:
    driver: local
